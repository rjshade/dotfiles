source /etc/profile

for file in ~/.zsh/zsh.d/*; do
  source $file
done

# any settings specific to this machine
if [[ -f ~/.config/local/zshrc-local ]]; then
  source ~/.config/local/zshrc-local
fi

# FuzzyFinder
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh
