[user]
  name = Robbie Shade
  email = robbie.shade@gmail.com
[include]
  # Override email on work machine(s).
  path = ~/.config/local/gitconfig
[color]
  ui = auto
  status = auto
  branch = auto
  interactive = auto
[advice]
  statusHints = false
[core]
  excludesfile = ~/.gitignore
  autocrlf = false
  filemode = false
[push]
  default = simple
[credential]
  helper = cache --timeout=3600
[alias]
  l = log --pretty=format:'%C(yellow)%h %C(green)%>(12)%ad %C(blue)%<(10)%an%C(red)%d %C(reset)%s' --date=relative --graph
  s = status
  co = checkout
  b = branch
  d = diff
